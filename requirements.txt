# ============================================================================
# SEMDT — Unified Requirements
# Covers: DT baseline, backbone swaps, lambda tests, LoRA runs,
#         instruction tuning runs, caption generation pipeline
# ============================================================================

# ── Core deep learning ──────────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0

# ── HuggingFace ecosystem ───────────────────────────────────────────────────
transformers>=4.40.0          # Qwen2, GPT-2, LLaMA, CLIP, BLIP-2 support
tokenizers>=0.19.0
accelerate>=0.28.0            # needed by some HF model loading paths
safetensors>=0.4.0

# ── LoRA / PEFT ─────────────────────────────────────────────────────────────
peft>=0.10.0                  # LoraConfig, get_peft_model, TaskType

# ── Data science ─────────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# ── Visualisation ────────────────────────────────────────────────────────────
matplotlib>=3.7.0

# ── Experiment tracking (optional — used in visiontrap pipeline) ─────────────
wandb>=0.16.0

# ── CLIP text encoder (used in semantic grounding stream) ────────────────────
# Install from OpenAI's repo if the pip package is unavailable:
#   pip install git+https://github.com/openai/CLIP.git
# Or use the HuggingFace CLIP wrapper which is included in transformers>=4.26
# clip @ git+https://github.com/openai/CLIP.git   # uncomment if needed

# ── BLIP-2 (offline caption generation only — not needed for training) ───────
# Already covered by transformers>=4.26 (Blip2Processor, Blip2ForConditionalGeneration)

# ── File I/O ──────────────────────────────────────────────────────────────────
pyarrow>=12.0.0               # parquet reading for text manifest
fastparquet>=2023.0            # alternative parquet backend
